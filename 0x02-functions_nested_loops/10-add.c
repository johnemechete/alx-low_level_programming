#include "main.h"
#include <stdio.h>

/**
* add - To  adds two integers and returns the result
* followed by a new line
* @a: takes an int value
* @b: takes an int value
* Return: return 0 if it is an Alphabet  case else 1
*/

int add(int a, int b)
{
	return (a + b);
}
